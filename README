== EnergyPet

git clone git://github.com:activefx/energy_pet.git
rename energy_pet <new_application_name>
git remote rename origin energy_pet
bundle install
rake development:install
git commit -a -m 'installed energy_pet'



# Build PhantomJS from source
  sudo apt-get install libqt4-dev libqtwebkit-dev qt4-qmake
  git clone git://github.com/ariya/phantomjs.git && cd phantomjs
  git checkout 1.3
  qmake-qt4 && make


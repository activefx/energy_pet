- content_for :head do
  = render :partial => "usage_chart"

.page-header
  %h1
    Meter Readings for Last Month
    %small 15 Minute Intervals

#usagechart

%table.table.table-striped
  %thead
    %tr
      %th Time
      %th Watt Hours
  %tbody
    - @readings.each do |reading|
      %tr
        %td= Date.parse(reading['timeStamp']).strftime("%A %b %d %Y %I:%M %p")
        %td= reading['value'].to_i

